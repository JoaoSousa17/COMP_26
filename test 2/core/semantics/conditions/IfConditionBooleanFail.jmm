package core.semantics.conditions;

class IfConditionBooleanFail {
    int f(int value) {
        int res;
        if (value) {
            res = 1;
        } else {
            res = 0;
        }
        return res;
    }
}
