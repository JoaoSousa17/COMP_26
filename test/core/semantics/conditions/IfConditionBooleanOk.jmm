package core.semantics.conditions;

class IfConditionBooleanOk {
    int f(boolean value) {
        int res;
        if (value) {
            res = 1;
        } else {
            res = 0;
        }
        return res;
    }
}
