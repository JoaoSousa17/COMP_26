package core.semantics.conditions;

class DoWhileConditionBooleanOk {
    int f(int limit) {
        int i;
        int res;
        res = 0;
        do {
            res = res + 1;
            i = i + 1;
        } while (i < limit);
        return res;
    }
}