package core.semantics.conditions;

class ForConditionBooleanOk {
    int f(int limit) {
        int i;
        int res;
        res = 0;
        for (i = 0; i < limit; i = i + 1) {
            res = res + 1;
        }
        return res;
    }
}